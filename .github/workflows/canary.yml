name: CI - Canary

on:
  push:
    tags:
      - '*'
    branches:
      - 'canary/**'
jobs:
  prod-check:
    # if: ${{ github.ref == 'refs/heads/canary/**' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{github.ref}}"
