import React, { ReactElement } from 'react';

import './button.css';

const Button = (): ReactElement => (
  <button className="ids-btn" type="submit">
    Button
  </button>
);

export default Button;
